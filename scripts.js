const vulpix = {
    species: 'vulpix',
    nickname: 'Lil fox',
    stats: {
        'attack:':41,
        'special attack': 50
    }
}

console.log(vulpix);
console.log(vulpix.nickname);
console.log(vulpix.stats["special attack"]);

vulpix.level = 5

console.log(vulpix);